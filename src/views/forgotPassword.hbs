<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .registration {
            max-width: 600px;
            width: 100%;
            margin: 0 auto;
            margin-bottom: 20px;
        }

        .msg-principal {
            color: #000;
            text-align: center;
            font-size: 23px;
            padding: 17px;
        }

        .text-principal {
            text-align: center;
            color: #000;
            font-weight: 600;
            margin: 22px;
        }

        .text-destaque {
            margin-bottom: 16px;
        }

        .text-code {
            font-size: 24px;
            margin-bottom: 10px;
            color: #000;
            font-weight: bold;
        }
        
        .text-info {
            color: #000;
            margin-bottom: 10px;
            font-size: 12px;
        }
        .text-auxiliar {
            font-weight: 400;
            max-width: 350px;
            margin: 18px auto;
        }
    </style>
</head>

<body>
    <div class="registration">
        <div class="msg-principal">
            <span>Solicitação de Recuperação de Senha</span>
        </div>

        <div class="text-principal">
            <p class="text-destaque"> Olá, <span> {{name}}</span> </p>

            <p class="text-destaque">Verifique seu código de recuperação da senha abaixo</p>

            <a class="text-code">{{code}}</a>

            <p class="text-info">Válido por 2 horas*</p>

            <p class="text-auxiliar">Atenciosamente</p>

            <p class="text-auxiliar">Equipe EvoFit</p>
        </div>
    </div>
</body>